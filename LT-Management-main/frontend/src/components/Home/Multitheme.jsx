
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#fca588', // Replace 'yourColor' with your desired color
    },
    // Other theme configurations...
  },
  // Other configurations like typography, spacing, etc...
});

export default theme;